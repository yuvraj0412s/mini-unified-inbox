# Mini Unified Inbox System

A FastAPI project that simulates a sales inbox. It groups emails into threads, assigns SDRs, tracks lead status, and monitors SLA.  

**Run:**  
```bash
pip install -r requirements.txt
uvicorn main:app --reload
```
loom video link
